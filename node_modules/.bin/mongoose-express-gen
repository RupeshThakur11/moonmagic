#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mongoose-express-generator/bin/mongoose-express-gen" "$@"
  ret=$?
else 
  node  "$basedir/../mongoose-express-generator/bin/mongoose-express-gen" "$@"
  ret=$?
fi
exit $ret
